/*var Container = function() {
	Widget.call(this);
	
	this.children = [];
};

Container.prototype = Object.constructPrototype(Widget);

Container.prototype.add = function(widget) {
	this.children.push(widget);
	return true;
};

Container.prototype.remove = function(widget) {
	var index;
	if( (index = this.children.indexOf(widget)) > -1 ) {
		this.children.splice(index, 1);
		return true;
	}
	return false;
};

*/
